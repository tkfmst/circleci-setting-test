#[cfg(test)]
mod tests {
    #[test]
    fn it_works_1() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
    #[test]
    fn it_works_2() {
        let result = 1 + 1;
        assert_eq!(result, 2);
    }
}
